extends layouts/main

block content
    .search__header
        h3 Searching for: #{searchingBy}
    .search__videos
        +videoBlock({
            title:item.title,
            views:item.views,
            videoFile:item.videoFile
        })